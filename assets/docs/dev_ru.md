# About extend

## Shortcuts: 
Список горячих клавиш...

#### For MAP
- [ M ] - скрывает / отображает панель MAP
- [ С ] - переключает отображения MAP (glyphs / components)
- [ ctrl + F ] - скрывает / отображает поисковой элемент MAP


#### For globalCanvas
- [ space ] - переключает режимы Canvas (free / align on spline)
- [ ctrl + A ] - выделяет все элементы на полотне
- [ delete ] или [ backspace ] - удаляет элемент с полотна

** в режиме "align on spline" зажатый [ shift ] при перемещении опорной точки <br>
позволяет двигать весь сплайн целиком.


#### For Glyph Preview
- [ S ] - открывает информацию о глифе. Alt: [ I ]
- [ F ] - режим редактирования дельт (f-type) ( из условия fenerative font)
- [ G ] - режим редактирования Guidelines
- [ H ] - режим редактирования точек (horner). Alt: [ P ], [ B ] 


// При условии процедурного подхода и активного режима (f-type)
- [ shift ] (toggle) - снэпинг к констрейнам из условия, что они присутствуют в рецепте

// В режиме редактирования точек (horner)
- [ ctrl + z ] - undo
- [ ctrl + y ] - redo
- [ ctrl + v ] - full rewrite paste from SVG buffer 

<br><br>

### Guidelines: 
Направляющие линии (регулируют метрики шрифтового файла)

<br><br>

## Структура: 
// В процессе

```
fe\ ← Корневая директория
 │
 ├─ index.html   ← (DOC)
 ├─ style.css    ← (стили страницы)
 ├─ main.js      ← (логика взаимодействия с пользователем / бизнес логика)
 │
 ├─ assets\      ← Сторонние файлы на странице
 │     ├─ docs\..
 │     ├─ img\..
 │     └─ svg\..
 │
 ├─ libs\        ← Сторонние минифицированные библиотеки
 │
 ├─ core\        ← отдельные js модули + классы
 │   │      
 │   ├─ init.js  ← модуль отвечающий за логику при загрузке 
 │   ├─ utils.js ← модуль содержащий вспомогательные функции
 │   │
 │   ├─ ProcedureGlyph.js  ← внутренние классы
 │   └─ GlyphRecipes.js    ← готовые рецепты (шаблоны)
 │
 ├─ dev\              ← Папка содержащие файлы разработки ( присутствует в git ветке fev\ )
 │   │
 │   ├─ !backup\      ← Full Backups folder
 │   │    ├─ ...\
 │   │    └─ ...\   
 │   │
 │   ├─ comments\     ← Прямые комментарии
 │   ├─ debug\        ← Файлы используемые для дебага
 │   ├─ design\       ← Файлы дизайна (psd, ai, etc...)
 │   ├─ docs\         ← Файлы под документацию
 │   └─ js\           ← js temp folder: прототипы / сегментированные по логике блоки
 │
 ├─ README.md   ← вводная информация под github
 └─ LICENSE     ← шаблон лицензии от github

```

<br><br>

## Классы:
// В процессе


### ProcedureGlyph.js

- #### ShapeData
Определяет "сплайн" / "кривые" в проекте
- нужен для рисования кривых типа bspline / bezier-cubic
- epts хранит регулирующие точки
- pathPoints хранит набор равномерно распределённых точек вдоль кривой

<br>

- #### PenLine
Определяет элементы управления StemLine (промежуточные/опорные контроллеры)

<br>

- #### StemLine
Определяет процедурный штрих
- логика constrains реализована здесь через гетеры

<br>

- #### ProcedureGlyph
Определяет процедурный глиф 
- хранит рецепт
- имеет метод конвертации в openType glyph path (с нормализацией через paper.js)


<br><br>











