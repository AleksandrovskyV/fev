https://redketchup.io/icon-converter
https://favicon.im/ru/convert/svg-to-favicon

<link rel="manifest" href="./assets/manifest">
manifest.json ???
  "theme_color": "#ffffff",
  "background_color": "#ffffff",
  "display": "standalone"
}


/// Что требуется сейчас: 
- обёртка над readme
- вынести все проверки документа + стартовые методы в отдельный js

- не нравится анимационный движок (куча лишних флагов)

- Изменение Guidelines (обычная запись в файл) + ascender&descender change in generateFont
+ Почти ( база готова ) ? проблемы: 
нет информации пользователю, что у всего семейства меняются метрики (это непонятно), так как 
тайлы генерируются с учетом сдвига (центрирование)
- проверить экспорт метрик после изменения guidelines


Simple Font:
- в Bezier режиме (GroupTransform) - полностью нерабочий в обоих ветках
- Специальные символы шрифта должны отображаться со своим уникальный css стилем (+ желательно какой то текст (как пример space symbol)

Global Interface:
+ Анимация прозрачности интерфейса при первом клике
+ ctrl + A - в globalCanvas - выделение всех плиток
- Добавить экспорт SVG из globalCanvas (всего полотна)
- Добавить Play Button ?

Spline Mode:
- Выравнить букву по центру (класс игрока?) 
+ временный костыль в globalCanvasRender gctx.translate(-glyphAW / 2, 0)
- Подумать над способом хранения\изменения позиции на сплайне (чтобы не трогать global offset)
+ (clickOffsetDelta, alignTotalOffset, customAlignOffset)
- Перестановка буква средством перемещения мыши
+ testLine.getClosestProgress(mx, my) + clickOffsetDelta: mousePosPx - o.customAlignOffset 
- gcenter заменить на вторую точку (вместо center pos) - она не берётся на старте
+ замена на смещение через shift (любой точки) центр скрыт
- при изменение ползунков\guideline пересчёт положения
+ recordShapeSequence() + updateShapeTracking() *имей ввиду что updateTracking() для align жесток и всё может разрушить
- spline world offset при изменении положении окна
+ изменили всю структуру внедрив объект world для трансформации мира ( потом следует заменить getCanvasMousePosOrig > getCanvasMousePosAlt)
- Анимация bspline на старте
+ Собрал темплейт, желательно почистить тайминги (Почистил тайминги)

- добавить глобальную кнопку reset в gCanvasBtns (чтобы можно было привести сплайн в исходный вид)
= пока лишь условный метод (нужно прописать логику в двух кейсах)

- важного заранее продумать ротацию через прокрутку колёсика(мы меняем внутрениий угол в bbox) + deltas 


Procedure Glyph:
- reset deltas при переходе на обычный режим + переключение на другую букву - не убирает кнопку (т.е. кнопка отображается там где не должна быть)
~ change delta > horner mode > change glyph

- Буква О (Вопрос открытый)
- Взаимодействие с анкор точками penLines (procedure class) (реализация уже придуманного расписанного решения). 
- Отстуствие на текущий момент цепочки работы с компонентами
- хранение кастомного пути в Procedure Glyph (Получается подуровень дельт?)
- Нет реализация snap to guidelines (сами guidelines лучше вывести в отдельный класс). 


Secondary Tasks:
- Проверить simple font \ procedure 
- добавить метод возврата точки (чтобы мы могли выравнить линию по сплайну, а потом получить её x\y чтобы сделать realing (y-case)


Backend Devopment Notes:
- Проверить работоспособность main.js в качестве модуля
- Сделать единую ветку интеграции codeMirror (fallback on source if not connected) - это упростит работу с паралельной работой
- заменить все find name на find unicode (это точнее работает с символами). особенно касается массива gCanvasObject
-


==

Что мы сделали:
0. Создали массив GLYPH_DELTAS который хранит внесённые пользователем изменения. 
1. Создали метод "updateProcedureGlyphDeltas()" который реализует инициализацию и обновление (GLYPH_DELTAS). 
2. Интегрировали чтение GLYPH_DELTAS в класс StemLine, чтобы пользователь мог взаимодействать и изменять rawPts (через GLYPH_DELTAS) в рамках Canvas 
(* Дельты хранятся в формате glyph units)
3. shiftMode (допустим он активируется клавишей shift) - вместо применении delta - меняли параметр t у constrains.
4. - плитки не переносятся на canvas при инициализации simple font (логика работает лишь после "generate font" и заменой его через dragandfrop на simple font ) >  исправлено

5. Добавить аналогичное поведение для метода getOpenPath (Как в checkHit) - чтобы путь под экспорт рисовался с учётом GLYPH_DELTAS.
- Временное решение: Добавлен новый метод в класс ProcedureGlyph (не умеющий пока в нормализацию) + commitGlyphEdits() на mouseup (чтобы сохранения обновились).
- Необходимое решение: интегрировать весь generateGlyphPath (там несколько версий) из AppMain.js в класс ProcedureGlyph. И в нём же решить задачу обновления дельт.

==


Честно скажу - мне не нравится твоё структурное предложение.
Ты интегрируешь часть прописанной логики constrain в mousemove (которая отвечает в основном как метод раздачи флагов и вызовов).

К сожалению это плохая реализация и усложняет структуру, но пока это рабочий вариант. 
Я считаю это плохим, так как мы много лезем во внутреннюю структуру класса StemLine - делая запрос на pt.origin - который плохо 
читается из представленного метода. Конечно мы можем переименовать origin и сделать реструктуризацию, но это нужно делать вдумчиво, 
на этой сейчас просто нет времени. Давай мы ограничим, что t не мог быть больше 1 и 0 в 


// History

У меня есть ползунок который отвечает за изменение всех advanceWidth у глифов.
Нам нужно собрать метод, который мы будем вызывать с такой логикой:
updateShapeTracking(){
1. Строим карту всех элементов на shape 
2. Находим первый элемент на shape учитывая все пользовательские смещения.
2. Берём первую букву как anchor и сдвигаем все элементы вправо с учётом обновленного advancedWidth
}
Что даёт нам такой метод? Допустим пользователь расставил буквы, с нужным его трекингом и он доволен. 
Меняет advancedWidth > запускается updateShapeTracking(  и все символоы аккуратно сдвигаются, учитывая
обновлённый advancedWidth.

====

Нам нужно создать с нуля или обновить текущий ключ в GLYPH_DELTAS.
Сами данные для ключя взять с объекта currentProcedureGlyph (это переменная). Вот его структура

advanceWidth: 580
symbol: "А" // основной ключ
elements: (3) [StemLine, StemLine, StemLine] // те самые индексы
recipeFn: ƒ anonymous(p,self )


let GLYPH_DELTAS = {
    A: {
        0: { pts: [], w: 10, pAngle: 0 }, 
        1: { pts: [], w: 12, pAngle: 5 }
    },

    I: { // NotName in Class now, its example
        StemI: { pts: [], w: 8, pAngle: 0 } 
    },
};

const char = "A";
const ptsA = GLYPH_DELTAS[char][0].pts; 
const ptsI = GLYPH_DELTAS["I"].StemI.pts;

///



Complete
2.1 Сохранение выбранных элементов в поиске (при прокрутке variables сбивается) +
2.4 Общий Handle метод + Шорткад на GMAP +
2.6 Rect Selector сбивает когда заходит на GMAP +
3. Добавить иконку глазика в CMAP рядом с текстом (пока не глазик, но +

Неясно
2.2 - При изменении ?чего? в режиме компонентов идёт переключение, что тоже не камильфо 
(при этом если отключить GMAP - Глобальный Canvas всё обновляет. ? Возможно исправлено
2.3 Стили на перемещение? - я не понял этой правки возможно исправлено
2.5 При drag and drop - Hide other tile ( or retransparency ) ?


При измении иногда не обновляется
- Если добавить глифов на canvas и перейти компоненты - отоброжает что нет активного глифа, но ползунки доступны (меняю canvas)
c: это логично но сбивает
-! Также если на canvas объекты и ты кликаешь по ним: обратно в glyph Editor он не возвращается. (* Вся цепочка взаимодействия с кликом изменена)
c: пока неясно хорошо это или плохо (возможно будет хорошо, если просто хочется трансформировать)


<div id="cmpaScrollWrapper">
	<div id="cmap">
		<div class="tile" data-name="А" data-array-index="0" draggable="true">
		<div class="tile" data-name="Н" data-array-index="1" draggable="true">
		<div class="tile" data-name="у" data-array-index="2" draggable="true">
	</div>
</div>

////

Процесс Feneration
Свойство Fenerative
Объект Fenerative
fenerate - "действие"
fenerative - "прилагательное"